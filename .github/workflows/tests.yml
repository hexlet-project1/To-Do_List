name: tests

on:
  pull_request:
    types: [opened, edited, reopened]
    branches:
      - "*"
  merge_group:
    branches:
      - "*"

jobs:
  frontend-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        commands: [lint, prettier, jest]
    defaults:
      run:
        working-directory: ./frontend
    steps:
      - uses: actions/checkout@v4
      - name: install dependencies
        run: npm ci
      - name: ${{ matrix.commands }}
        run: npm run ${{ matrix.commands }}
