[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1.0",
    "psycopg[binary]>=3.2.7",
    "sqlalchemy>=2.0.40",
]
